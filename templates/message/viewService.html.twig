{% block body %}
<style>
</style>
<div class="example-wrapper">
        {% if app.user %}
            <ul>
                <li>{{ app.user.fullname }}</li>
                <li><a href="{{ path('logout') }}">Logout</a></li>
                <h1><a href="{{ path('inbox') }}">Recibidos</a></h1>
                <h1><a href="{{ path('send') }}">Enviados</a></h1>
                <h1><a href="{{ path('create') }}">Crear</a></h1>
            </ul>
        {% else %}
            <h1>NO ESTAS LOGEADO</h1>
            <li><a href="{{ path('login') }}">Iniciar session</a></li>
            <li><a href="{{ path('register') }}">Log in session</a></li>
        {% endif %}
        
</div>
{% endblock %}
